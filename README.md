Use LevelDB has a fact cache to reduce memory usage of Ansible.

This will trade speed for reduced memory usage.

The goal of this project is to determine how much speed is lost and how much
memory is conserved.
